/*
 * NTHU EE2310 - 題目 14685: Sum of four-digit integer
 *
 * 【課本參考】《由片語學習C程式設計（第二版）》
 *   第 3 章（運算）
 *   如果不知道如何解題，請參考課本第 3 章的說明。
 *
 * 【題目連結】https://acm.cs.nthu.edu.tw/problem/14685/
 *
 * 【題目說明】
 * 輸入一個四位整數，計算並輸出各位數字之和。
 * 例如輸入 1234，各位數字為 1、2、3、4，總和為 10。
 *
 * 【解題策略】
 * 1. 讀入一個四位整數 n
 * 2. 利用整數除法和取餘數來分解各位數字：
 *    - 千位數：n / 1000
 *    - 百位數：(n / 100) % 10
 *    - 十位數：(n / 10) % 10
 *    - 個位數：n % 10
 * 3. 將四個位數相加後輸出
 */
#include <stdio.h>

int main(void) {
    int n;        /* 輸入的四位整數 */
    int d1;       /* 千位數字 */
    int d2;       /* 百位數字 */
    int d3;       /* 十位數字 */
    int d4;       /* 個位數字 */
    int sum;      /* 各位數字之和 */

    /* 讀入四位整數 */
    scanf("%d", &n);

    /* 分解各位數字 */
    d1 = n / 1000;          /* 千位：整除 1000 取整數部分 */
    d2 = (n / 100) % 10;    /* 百位：先整除 100，再取餘數 10 */
    d3 = (n / 10) % 10;     /* 十位：先整除 10，再取餘數 10 */
    d4 = n % 10;            /* 個位：直接取餘數 10 */

    /* 計算各位數字總和 */
    sum = d1 + d2 + d3 + d4;

    /* 輸出結果 */
    printf("%d\n", sum);

    return 0;
}
