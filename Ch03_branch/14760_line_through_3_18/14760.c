/*
 * NTHU EE2310 - 題目 14760: Check if line passes through point (3,18)
 *
 * 【課本參考】《由片語學習C程式設計（第二版）》
 *   第 3 章：條件判斷
 *   如果不知道如何解題，請參考課本第 3 章的說明。
 *
 * 【題目說明】
 * 輸入四個整數 a, b, c, d 代表平面上兩點 (a, b) 和 (c, d)，
 * 判斷過這兩點的直線是否通過點 (3, 18)。
 * 輸出 "Yes" 或 "No"。
 *
 * 【解題策略】
 * 判斷三點 P1=(a,b)、P2=(c,d)、P3=(3,18) 是否共線，
 * 使用向量叉積法：
 *   向量 P1P2 = (c-a, d-b)
 *   向量 P1P3 = (3-a, 18-b)
 *   若叉積為零，則三點共線（直線過該點）
 *   叉積：(c-a)*(18-b) - (d-b)*(3-a) == 0
 *
 * 原理：三點共線的充要條件是任意兩向量的叉積為零，
 *       即行列式 | (c-a) (d-b) |
 *                | (3-a) (18-b)| 的值為零
 */
#include <stdio.h>

int main(void) {
    int a, b;    /* 第一個點的座標 (a, b) */
    int c, d;    /* 第二個點的座標 (c, d) */
    int cross;   /* 向量叉積的結果 */

    /* 讀入兩個點的座標 */
    scanf("%d %d %d %d", &a, &b, &c, &d);

    /* 計算向量叉積：P1P2 × P1P3
     * 向量 P1P2 = (c-a, d-b)
     * 向量 P1P3 = (3-a, 18-b)
     * 叉積 = (c-a)*(18-b) - (d-b)*(3-a)
     */
    cross = (c - a) * (18 - b) - (d - b) * (3 - a);

    /* 若叉積為零，表示三點共線（直線過點 (3,18)） */
    if (cross == 0) {
        printf("Yes\n");
    } else {
        printf("No\n");
    }

    return 0;
}
