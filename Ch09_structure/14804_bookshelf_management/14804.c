/*
 * NTHU EE2310 - 題目 14804: Bookshelf Management
 *
 * 【課本參考】《由片語學習C程式設計（第二版）》
 *   第 9 章（結構）
 *   如果不知道如何解題，請參考課本第 9 章的說明。
 *
 * 【題目說明】
 * 模擬一個有 8 個書槽的書架（類似 LRU 快取）。書籍編號為 1～255。
 * 每次取書（讀書號 b）的規則：
 *   - 若 b 已在書架上：將 b 從原位置移除，放到最右端
 *   - 若 b 不在書架上：
 *     - 書架未滿（不足 8 本）：直接將 b 放到最右端
 *     - 書架已滿（8 本）：移除最左端的書，將 b 放到最右端
 * 初始書架為空。
 * 處理完所有取書操作後，輸出書架上 8 個位置的書號（空位輸出 0）。
 *
 * 【解題策略】
 * 用長度為 8 的整數陣列模擬書架，另維護一個變數 cnt 記錄目前書本數量。
 * - 「移除書籍」：找到位置後將後面的書往左移（陣列左移）。
 * - 「加入最右端」：在 cnt 位置放入書號，cnt 加一。
 * - 「移除最左端」：等同刪除位置 0（整個陣列左移一格）。
 *
 * 輸入：一連串書號，直到 EOF
 * 輸出：書架上 8 個位置的書號，以空白分隔（一行）
 */
#include <stdio.h>

#define SHELF_SIZE 8   /* 書架最大容量 */

int main(void) {
    int shelf[SHELF_SIZE];   /* 書架陣列（0 表示空位） */
    int cnt = 0;             /* 目前書架上的書本數量 */
    int b;                   /* 讀入的書號 */

    /* 初始化書架為空 */
    for (int i = 0; i < SHELF_SIZE; i++) {
        shelf[i] = 0;
    }

    /* 逐一讀入書號，直到 EOF */
    while (scanf("%d", &b) == 1) {

        /* 先搜尋 b 是否已在書架上 */
        int pos = -1;   /* b 在書架上的位置，-1 表示不在 */
        for (int i = 0; i < cnt; i++) {
            if (shelf[i] == b) {
                pos = i;
                break;
            }
        }

        if (pos >= 0) {
            /* b 已在書架上：從原位置移除，放到最右端 */
            /* 將 pos+1 以後的書往左移一格 */
            for (int i = pos; i < cnt - 1; i++) {
                shelf[i] = shelf[i + 1];
            }
            cnt--;   /* 移除後書本數量減一 */
        } else {
            /* b 不在書架上 */
            if (cnt == SHELF_SIZE) {
                /* 書架已滿：移除最左端（位置 0），整體左移 */
                for (int i = 0; i < SHELF_SIZE - 1; i++) {
                    shelf[i] = shelf[i + 1];
                }
                cnt--;   /* 移除後書本數量減一 */
            }
            /* 書架未滿（或已騰出空間）：不需額外處理 */
        }

        /* 將 b 放到最右端 */
        shelf[cnt] = b;
        cnt++;
    }

    /* 輸出書架上 8 個位置的書號（空位補 0） */
    for (int i = 0; i < SHELF_SIZE; i++) {
        if (i > 0) printf(" ");
        if (i < cnt) {
            printf("%d", shelf[i]);
        } else {
            printf("0");   /* 空位輸出 0 */
        }
    }
    printf("\n");

    return 0;
}
